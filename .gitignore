# python venv (large CAD kernel)
cqenv/
__pycache__/
*.log

# scratch renders / intermediate crops at repo root (kept in images/ instead)
/*.png
/*.svg

# trace intermediates (regenerated by the pipeline; geometry.json is the source of truth)
_*.npy
_ears_mm.json
_holes2.json
_holes_raster.json
_tabs.npy
